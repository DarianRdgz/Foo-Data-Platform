spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fdp
    username: Dsters
    password: "$t3ll@g1G!"

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate

kroger:
  creds:
    required: false